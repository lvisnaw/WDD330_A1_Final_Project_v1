const tempF = parseFloat(document.getElementById('temp').textContent);const wSpeed = parseFloat(document.getElementById('speed').textContent);let chill = 35.74 + (0.6215 * tempF) - (35.75 * Math.pow(wSpeed, 0.16)) + (0.4275 * tempF * Math.pow(wSpeed, 0.16));chill = Math.round(chill);if (tempF <= 52 && wSpeed > 3) {document.getElementById("chill").textContent = chill+"\xB0F";} else {document.getElementById("chill").textContent = "*";}